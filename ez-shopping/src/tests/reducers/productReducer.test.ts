import { productReducer } from "../../redux/reducers/productReducer";
import { Product } from "../../types/product";

const initialState: Product[] = [];

describe("Test actions in productReducer", () => {
  test("If give intial state ta first", () => {});
});
